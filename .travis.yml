os:
  - linux

language: php

php:
  - '7.1'
  - '7.1.3'
  
before_script:
  - composer self-update
  - composer install 
  - pear install pear/PHP_CodeSniffer
  
  
script:
  - vendor/bin/phpunit --coverage-text